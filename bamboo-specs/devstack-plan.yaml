---
version: 2
#server-name: DevStack Bamboo#
plan:
  project-key: TEST
  key: FIT
  name: first
stages:
  - Default Stage:
      manual: false
      final: false
      jobs:
        - Default Job
Default Job:
  key: JOB1
  tasks:
    - script:
       interpreter: SHELL
       scripts:
         - echo "tesk1"
    - script:  echo "task2"  
    - script: echo "task3" > deployment.yaml
    - docker:
       Command: Build a Docker image
       Repository: deaamoflih/test19:3.33
       artifacts: 
         - artri
 #   - Docker: 
 #      Command: Push a Docker image
 #      Repository: deaamoflih/test19
      
  artifacts:
    - name: Openshift deployment descriptor
      location: ./
      pattern: deployment.yaml
      shared: true
      required: true        
  requirements:
    - system.docker.executable
    - DOCKER
branches:
  create: for-new-branch
  delete:
    after-deleted-days: 7
    after-inactive-days: never
  link-to-jira: true
labels: []
other:
  concurrent-build-plugin: system-default

